# Analog-Digital-Clock-Java-AWT-
A real-time clock application built in Java using AWT and Swing, displaying both analog and digital time formats. The analog clock features a custom purple gradient face, Roman numerals (I-XII), and smoothly moving second, minute, and hour hands. The application handles dynamic resizing and uses a Timer to refresh the display every second.
